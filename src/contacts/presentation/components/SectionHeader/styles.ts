import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 24,
    paddingVertical: 6,
    lineHeight: 18,
    fontSize: 14,
    fontWeight: 'bold',
    textAlign: 'right',
  },
});
