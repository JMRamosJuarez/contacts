import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: { flex: 1 },
  img: {
    width: 200,
    height: 200,
    borderRadius: 200,
    marginVertical: 24,
    alignSelf: 'center',
  },
});
